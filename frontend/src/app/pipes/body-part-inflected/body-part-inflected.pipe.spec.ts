import { BodyPartInflectedPipe } from './body-part-inflected.pipe';

describe('BodyPartInflectedPipe', () => {
  it('create an instance', () => {
    const pipe = new BodyPartInflectedPipe();
    expect(pipe).toBeTruthy();
  });
});
