import { PersonalNumberValidatorDirective } from './personal-number-validator.directive';

describe('PersonalNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonalNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
